# Python - Network #0

## Description
This project focuses on HTTP requests and network fundamentals using bash scripts and `curl`.

## Tasks

### 0. cURL Body Size
Write a Bash script that takes in a URL, sends a request to that URL, and displays the size of the body of the response in bytes.

* **File:** `0-body_size.sh`
* **Usage:** `./0-body_size.sh <URL>`
* **Example:**
```bash
$ ./0-body_size.sh 0.0.0.0:5000
10
